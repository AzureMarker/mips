use crate::ast::*;

grammar;

pub Program: Program<'input> = Item* => Program { items: <> };

Item: Item<'input> = {
    <d:Directive> => Item::Directive(d)
};

Directive: Directive<'input> = "." <name:r"[a-z]+"> => Directive(name);
